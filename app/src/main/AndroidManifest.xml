<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeathEvent"
        tools:targetApi="31">
        <activity
            android:name=".view.PronosticoActivity"
            android:exported="false" />
        <activity
            android:name=".view.AddEventActivity"
            android:exported="false"
            android:label="@string/title_activity_add_event"
            android:theme="@style/Theme.WeathEvent" />
        <activity
            android:name=".view.login.IniciarSesionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.home.HomeActivity" />
        <activity android:name=".view.EventDetailsActivity" />
        <activity android:name=".view.RegistroActivity" />
        <activity android:name=".view.PerfilActivity" />
    </application>

</manifest>